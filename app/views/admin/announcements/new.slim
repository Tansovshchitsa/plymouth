= simple_form_for @announcement,:validate => true,url: admin_announcements_path(@announcement) do |f|
  = f.input :content, :as => :ckeditor, :input_html => { :ckeditor => {:toolbar => 'Full'} }
  = f.input :author 
  = f.submit "提交"