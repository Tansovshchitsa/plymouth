= render_cell 'messages', :index, :message=>flash[:notice]
.widget
  h4
    i.icon-list-alt
    span.text-info =t('system.enroll_info')
  .widgetarea 
    table class="table table-hover table-bordered"
      thead
        tr
          th 用户名
          th 邮箱
      tbody
        - @enroll.customers.each do |customer|
          td = customer.email
         